get:
  operationId: apiPuzzleDaily
  summary: Get the daily puzzle
  description: |
    Get the daily Lichess puzzle in JSON format.
  tags:
    - Puzzles
  security: []
  responses:
    "200":
      description: The daily puzzle.
      headers:
        Access-Control-Allow-Origin:
          schema:
            type: string
            default: "'*'"
      content:
        application/json:
          schema:
            $ref: "../../schemas/PuzzleAndGame.yaml"
          examples:
            default:
              $ref: "../../examples/puzzles-getDailyPuzzle.json.yaml"
