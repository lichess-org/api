post:
  operationId: challengeDecline
  summary: Decline a challenge
  description: |
    Decline an incoming challenge.
  tags:
    - Challenges
  security:
    - OAuth2: ["challenge:write", "bot:play", "board:play"]
  parameters:
    - in: path
      name: challengeId
      schema:
        type: string
        example: "5IrD6Gzz"
      required: true
  requestBody:
    description: Details related to decline of challenge
    required: false
    content:
      application/x-www-form-urlencoded:
        schema:
          type: object
          properties:
            reason:
              type: string
              description: Reason the challenge was declined. Only the values listed below are accepted; any other value falls back to `generic`. The reason will be translated to the player's language.
              enum:
                - generic
                - later
                - tooFast
                - tooSlow
                - timeControl
                - rated
                - casual
                - standard
                - variant
                - noBot
                - onlyBot
  responses:
    "200":
      description: The challenge was successfully declined.
      headers:
        Access-Control-Allow-Origin:
          schema:
            type: string
            default: "'*'"
      content:
        application/json:
          schema:
            $ref: "../../schemas/Ok.yaml"
          examples:
            default:
              $ref: "../../examples/challenges-declineChallenge.json.yaml"
    "404":
      description: The challenge to decline was not found.
      content:
        application/json:
          schema:
            $ref: "../../schemas/NotFound.yaml"
