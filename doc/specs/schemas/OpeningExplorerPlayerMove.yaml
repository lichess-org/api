type: object
properties:
  uci:
    type: string
  san:
    type: string
  averageOpponentRating:
    type: number
  performance:
    type: number
  white:
    type: number
  draws:
    type: number
  black:
    type: number
  game:
    oneOf:
      - type: 'null'
      - type: object
        properties:
          id: 
            type: string
          winner:
            type: [string, 'null']
            enum:
              - white 
              - black
          speed:
            $ref: './Speed.yaml'
          mode:
            type: string
            enum:
              - casual
              - rated
          white:
            $ref: './OpeningExplorerMasterPlayer.yaml'
          black:
            $ref: './OpeningExplorerMasterPlayer.yaml'
          year:
            type: number
          month:
            type: string
