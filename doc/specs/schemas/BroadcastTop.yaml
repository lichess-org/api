type: object

properties:
  active:
    type: array
    items:
      $ref: "./BroadcastWithLastRound.yaml"
  upcoming:
    type: array
    items:
      $ref: "./BroadcastWithLastRound.yaml"
    minItems: 0
    maxItems: 0
    deprecated: true
  past:
    type: object
    properties:
      currentPage:
        type: integer
        example: 4
      maxPerPage:
        type: integer
        example: 20
      currentPageResults:
        type: array
        items:
          $ref: "./BroadcastWithLastRound.yaml"
      previousPage:
        type: [integer, "null"]
        example: 3
      nextPage:
        type: [integer, "null"]
        example: 5
