type: object
properties:
  flag:
    type: string
  location:
    type: string
  bio:
    type: string
  realName:
    type: string
  fideRating:
    type: integer
    description: only appears if a user has set them
  uscfRating:
    type: integer
    description: only appears if a user has set them
  ecfRating:
    type: integer
    description: only appears if a user has set them
  cfcRating:
    type: integer
    description: only appears if a user has set them
  rcfRating:
    type: integer
    description: only appears if a user has set them
  dsbRating:
    type: integer
    description: only appears if a user has set them
  links:
    type: string
